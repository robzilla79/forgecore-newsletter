# DELETED 2026-05-13 — legacy standalone deploy, already disabled since 2026-05-02.
# Active deploy path: generate.yml / rebuild-site.yml -> Cloudflare Pages.
name: Deploy FORGE/DAILY Site (REMOVED)
on:
  workflow_dispatch:
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
