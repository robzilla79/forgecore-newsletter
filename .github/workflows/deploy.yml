name: Deploy FORGE/DAILY Site (disabled)

# Disabled on 2026-05-02.
# Reason: legacy standalone deploy duplicated the active AM/PM generator path.
# Active deployment now happens inside .github/workflows/generate.yml after publish verification.
# Keep this file as an audit marker so the workflow can be restored intentionally if needed.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This legacy standalone deploy workflow is disabled."
          echo "Active path: generate-am.yml / generate-pm.yml -> generate.yml -> publish_site.py -> verify_publish.py -> Cloudflare Pages deploy."
          echo "No deploy was attempted by this disabled workflow."
