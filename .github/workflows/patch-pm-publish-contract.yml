# DELETED 2026-05-13 — disabled noop, no longer needed.
name: Patch PM Publish Contract (REMOVED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is permanently removed.'
        required: false
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
