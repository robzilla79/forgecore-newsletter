name: Patch PM Publish Contract (DISABLED)
# Disabled — clean pipeline doesn't need patching
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
