# DELETED 2026-05-13 — hourly improvement loop disabled since 2026-05-02.
# Improvement now runs inline inside generate.yml via improve_until_passes.py.
name: Continuous Improvement Loop (REMOVED)
on:
  workflow_dispatch:
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
