name: Em Recover — AM Send

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  recover-am-send:
    uses: ./.github/workflows/send.yml
    secrets: inherit
    with:
      issue_slot: am
