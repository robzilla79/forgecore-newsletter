# DELETED 2026-05-13 — disabled noop, replaced by send-am.yml.
name: Recover AM Send (REMOVED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is permanently removed. Use send-am.yml.'
        required: false
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
