name: Recover PM Send (DISABLED)
# Disabled — replaced by clean 4-step pipeline
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use send-pm.yml."
