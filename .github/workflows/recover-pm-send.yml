name: Em Recover — PM Send

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  recover-pm-send:
    uses: ./.github/workflows/send.yml
    secrets: inherit
    with:
      issue_slot: pm
