# DELETED 2026-05-13 — disabled noop, replaced by send-pm.yml.
name: Recover PM Send (REMOVED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is permanently removed. Use send-pm.yml.'
        required: false
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
