# DELETED 2026-05-13 — disabled noop, replaced by generate-pm.yml.
name: Recover PM Issue (REMOVED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is permanently removed. Use generate-pm.yml.'
        required: false
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
