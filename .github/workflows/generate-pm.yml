name: Generate PM Newsletter Issue

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  generate-pm:
    uses: ./.github/workflows/generate.yml
    secrets: inherit
    with:
      issue_slot: pm
