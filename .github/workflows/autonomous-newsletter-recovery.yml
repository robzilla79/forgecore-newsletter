# DELETED 2026-05-13 — disabled noop, replaced by generate.yml pipeline.
# File retained as a tombstone to preserve git history reference.
name: Autonomous Newsletter Recovery (REMOVED)
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is permanently removed. Use generate-am.yml or generate-pm.yml.'
        required: false
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "removed"
