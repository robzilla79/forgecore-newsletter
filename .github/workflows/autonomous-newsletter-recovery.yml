name: Autonomous Newsletter Recovery (DISABLED)
# Disabled — replaced by clean 4-step pipeline in generate.yml
on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled. Use generate-pm.yml or generate-am.yml.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use generate-pm.yml or generate-am.yml."
